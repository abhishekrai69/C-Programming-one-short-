
// write a program to check if a number is divisible by 2 or not...

#include<stdio.h>
int main()
{
    int a;
    printf("Enter a number ");
    scanf("%d", &a);

    printf("%d", a % 2 == 0);
    return 0;
}


//write a program in c to print the average of 3 numbers.
// #include <stdio.h>
// int main() {
//     float num1, num2, num3, average;

//     // Input three numbers
//     printf("Enter three numbers: ");
//     scanf("%f %f %f", &num1, &num2, &num3);

//     // Calculate the average
//     average = (num1 + num2 + num3) / 3;

//     // Print the average
//     printf("The average of %.2f, %.2f, and %.2f is: %.2f\n", num1, num2, num3, average);
//     return 0;
// }

//write a program in c to check if a given character is digit or not

// #include <stdio.h>
// int main() {
//     char c;
    
//     // Prompt the user to input a character
//     printf("Enter a character: ");
//     scanf("%c", &c);
    
//     // Check if the entered character is a digit
//     if (c >= '0' && c <= '9') {
//         printf("%c is a digit.\n", c);
//     } else {
//         printf("%c is not a digit.\n", c);
//     }
    
//     return 0;
// }

